function build_project
{
  for tc_ta in test_app test_app_avr
  do
    for p in debug release test mtest profile doc
    do
      ( cd $tc_ta && make PROFILE="$p" ) || return $?
    done # for profile
  done # for toolchain test app

  return 0
}
